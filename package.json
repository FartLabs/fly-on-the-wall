{
  "scripts": {
    "start": "cd desktop && npm run start",
    "fmt": "prettier --write .",
    "fmt:check": "prettier --check .",
    "make": "cd desktop && npm run make"
  },
  "dependencies": {
    "prettier": "^3.8.1"
  }
}
